const controller1 = {// can put comment

		/** @inject("templates/template1.html") */
		template : null,
		controller: ()=>{
		}
}
const controller2 = {
		/** @inject("templates/template2.html") */
		template : null,
		controller: ()=>{
		}
}
const controller3 = {
		/** @inject("templates/template3.html") */
		template : [],
		controller: ()=>{
		}
}