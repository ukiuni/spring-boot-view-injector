const controller1 = {// can put comment
		template : $$inject("templates/template1.html"),
		controller: ()=>{
		}
}
const controller2 = {
		template : $$inject("templates/template2.html"),
		controller: ()=>{
		}
}
const controller3 = {
		template : $$inject("templates/template3.html"),
		controller: ()=>{
		}
}