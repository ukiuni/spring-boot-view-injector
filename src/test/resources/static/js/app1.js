const controller1 = {// can put comment

		/** @injectAsString("templates/template1.html") */
		template : null,
		controller: ()=>{
		}
}
const controller2 = {
		/** @injectAsString("templates/template2.html") */
		template : null,
		controller: ()=>{
		}
}
const controller3 = {
		/** @injectAsString("templates/template3.html") */
		template : [],
		controller: ()=>{
		}
}